# Diário — 2026-07-01

## O que foi feito

- Integração Postiz como engine de publicação invisível (cliente vê só "Nexu")
- Migrations criadas: `028_postiz_integration.sql`, `029_posts_social_account_id.sql`
- Edge Functions deployadas: `postiz-integrations` (OAuth + CRUD canais), `publish-scheduled-posts` (reescrita para Postiz API)
- Página `/canais` criada: grid de plataformas, OAuth popup, postMessage, polling fallback
- `PostDialog` reescrito: dropdown de canais em vez de plataformas
- Sidebar: item "Canais" adicionado
- OAuth callback: `GET /api/auth/postiz/callback` — parseia state=`brand_id|user_id`, salva em `social_accounts`
- Deploy na Vercel concluído (projeto `wacrm`, team `brizzis-projects`)
- Domínio `nexu.ia.br` + `www.nexu.ia.br` configurados na Vercel
- `NEXT_PUBLIC_SITE_URL=https://nexu.ia.br` atualizado
- Postiz OAuth App Redirect URL: `https://nexu.ia.br/api/auth/postiz/callback`
- DNS Cloudflare adicionado pelo usuário

## Decisões técnicas

- Postiz como backend invisível: clientes nunca veem nome "Postiz"
- `state=brand_id|user_id` no OAuth para rotear callback corretamente
- `social_account_id` em posts permite múltiplas contas da mesma rede por brand
- `access_token="postiz"` como placeholder (token real fica no Postiz)
- Plataforma: Nexu (nexu.ia.br)

## Problemas resolvidos

- Deploy Vercel: variáveis coladas no campo Key errado → usou ClipboardEvent paste no campo correto
- `nexu.com.br` → corrigido para `nexu.ia.br` em todos os lugares

## Pendências

- DNS propagação (pode levar até 30 min após sessão)
- Conectar redes sociais em `/canais` para testar OAuth flow completo
- Supabase migrations 028/029 aplicar em produção se não feito via MCP
- Testar publicação agendada end-to-end
