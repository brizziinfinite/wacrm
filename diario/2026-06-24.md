# Diário — 2026-06-24

## Sessão 15:54

### O que foi feito

**Fase 6 — Edge Functions**
- Deploy de `agent-1-strategist` (v10), `agent-2-roteirista` (v8), `publish-scheduled-posts` (v9) no Supabase projeto `xnwcalrlvjwszmtgkwfs`
- `_shared/llm-retry.ts` incluído como dependência em cada função
- `GEMINI_API_KEY` + `LLM_PROVIDER=gemini` configurados via Supabase Dashboard

**Fase 7 — Integrações CRM + Conteúdo**
- Content Package → WhatsApp Broadcast: botão "Enviar via WhatsApp" em `/packages/[id]` (pacote `approved`), dialog com seleção de template + audiência, dispara broadcast via `useBroadcastSending`, muda status para `converted_to_post`
- Conversations → Sources: migration `028_content_sources` criada, seção "Fonte de Conteúdo" adicionada no `contact-sidebar.tsx` do inbox, salva insights de conversa na tabela `content_sources` vinculados a `conversation_id` e brand opcional
- Contacts → Broadcast audience: já existia no Step2 do broadcast — nada a fazer

**Fase 8 — Finishing touches**
- BrandSwitcher criado (`src/components/brands/BrandSwitcher.tsx`) e integrado no sidebar, acima dos nav items de Conteúdo
- `/packages/[id]` criado com renderers por formato (carrossel, reel, story, blog, email, post), botões Aprovar/Rejeitar, integração WhatsApp
- `src/middleware.ts` renomeado para `src/proxy.ts` + export renomeado de `middleware` → `proxy` (Next.js 16.2.6)
- `supabase/` adicionado ao `exclude` do `tsconfig.json` (evitava erro de tipo Deno vs Node)
- Fix: `Button asChild` → `Button render=` em `calendar/page.tsx` e `posts/page.tsx`
- Build limpo: 41 páginas, zero erros TypeScript, zero warnings

### Decisões técnicas

- `content_sources` usa `source_ref` (text) para guardar `conversation_id` — genérico o suficiente para futuramente aceitar URLs também
- `publish-scheduled-posts` deployado com `verify_jwt: false` porque é chamado por cron, não por usuário
- Sidebar seção "Conteúdo" usa `BrandSwitcher` expansível com DropdownMenu — padrão `render=` do wacrm

### Problemas resolvidos

- TypeScript pegando arquivos Deno → excluir `supabase/` do tsconfig
- `Button asChild` não existe neste wacrm → `render=<Link />` padrão correto
- `middleware` deprecated no Next.js 16.2.6 → renomear arquivo + export
- Build encontrava 7 instâncias de `</div>\n  );\n}` → contexto ampliado para fazer edit único

### Pendências

- Sistema de créditos (Fase 2)
- Geração de imagens via `visual_prompt` (Fase 2)
- Notificação WhatsApp de aprovação (Fase 2)
- Geração de vídeo com Remotion (Fase 2, muito futuro)
- `/sources` — rota de listagem de `content_sources` (futuro)
- Reconexão de conta social expirada (futuro)
