# Diário — 2026-07-03

## O que foi feito

### Design Premium (toda a aplicação)
- Sidebar: logo gradiente ciano→índigo, nav ativo com glass gradient + glow
- Botão primário global: gradiente ciano→índigo + glow shadow + hover lift
- Cards globais: glass ring rgba, shadow profunda, hover lift + glow borda ciano
- Inputs globais: glass bg, focus glow ciano
- Header: backdrop-blur + gradient text + avatar gradiente
- GlowCard (`src/components/ui/glow-card.tsx`): spotlight que segue mouse via CSS vars `--glow-x` `--glow-y`
- Quick-actions: cada card envolto em GlowCard

### Página /relatorios
- Funil visual proporcional animado com barras que crescem no mount
- AnimatedNumber com requestAnimationFrame (counter animado)
- 4 KPIs: total leads, conversão, tempo médio, deal médio
- Seção diagnóstico AI via Gemini 2.5 Flash

### API /api/relatorios/diagnostico
- POST → Gemini 2.5 Flash REST
- Retorna: resumo, taxaConversao, gargalo, causasPravaveis[], pontoForte
- `responseMimeType: 'application/json'` forçado

### Fase 8.1 — Deal Automático via WhatsApp ✅
- Migration `030_auto_deal_config.sql`: coluna `auto_deal_pipeline_id` em `accounts`
- Webhook (`src/app/api/whatsapp/webhook/route.ts`): função `maybeCreateAutoDeal` fire-and-forget
  - Trigger: `isFirstInboundMessage === true`
  - Guard duplicata: verifica deal open existente no mesmo pipeline
  - Insere deal na primeira stage do pipeline (order by position)
- UI em `src/components/settings/deals-settings.tsx`: card "Lead automático via WhatsApp" com dropdown de pipelines

### Análise Whaticket Shell SaaS
- Analisado `/Users/brizzi/whaticket_shell_saas`
- Features úteis identificadas → planejadas como Fase 8 no Melhorias.md

## Decisões técnicas

- Gemini 2.5 Flash via REST (não SDK) — mais barato, já tinha GEMINI_API_KEY
- Fire-and-forget no webhook para nunca bloquear 200 OK da Meta
- `isFirstInboundMessage` flag (já computado no webhook) como trigger de auto-deal
- Branch de trabalho: `relatorios-funil-vendas`

## Pendências (próxima sessão)

### 8.2 — Rastreamento de tempo por etapa (PRÓXIMO)
- Migration `031_deal_tracking` — tabela `deal_stage_history`
  - Colunas: `deal_id UUID`, `stage_id UUID`, `entered_at TIMESTAMPTZ`, `left_at TIMESTAMPTZ`, `duration_seconds INTEGER`
- Trigger PL/pgSQL: `UPDATE deals SET stage_id` → insere em `deal_stage_history`
- Exibir tempo médio por etapa em `/relatorios`
- Highlight visual na etapa com maior tempo (gargalo real)

### 8.3 — Notas internas no deal
- Migration `032_deal_notes`
- UI no contact-sidebar

### 8.4 — Campos customizados de contato
- Migration `033_contact_custom_fields`

### 8.5 — NPS ao fechar deal
- Migration `034_deal_ratings`

### 8.6 — Agendamento de mensagens
- Migration `035_scheduled_messages` + Edge Function cron

### 8.7 — Chatbot menu árvore

### 8.8 — Integração Typebot

## Contexto importante

- Branch ativa: `relatorios-funil-vendas`
- Banco Supabase: projeto `xnwcalrlvjwszmtgkwfs` (Publik)
- Migration 030 CRIADA localmente mas precisa ser aplicada no Supabase remoto
- GEMINI_API_KEY já configurada no `.env.local`
- Caveman ultra + Ponytail full ativos nas sessões
