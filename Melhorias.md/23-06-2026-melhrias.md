# Melhorias — wacrm + Publik Fusion

## Status Geral

Produto: CRM WhatsApp + Plataforma de Conteúdo AI (fusão wacrm + Publik)
Base: `/Users/brizzi/wacrm/wacrm`
Banco: Supabase projeto Publik (`xnwcalrlvjwszmtgkwfs`)

---

## Concluído

### Banco de Dados
- [x] Migrations 001-026 wacrm aplicadas no banco Publik
- [x] Migration 027 — tabelas Publik migradas para `account_id` multi-tenant
- [x] `profiles` adaptado (adicionado `user_id`, `account_id`, `account_role`)
- [x] `is_account_member()` SECURITY DEFINER ativa em todas as tabelas
- [x] `accounts` table com backfill — 1 conta criada
- [x] `.env.local` criado com todas as chaves
- [x] Migrations 030-040 — CRM inteligente + Radar (40 migrations total)

### Infraestrutura
- [x] Decisão arquitetural: base = wacrm, módulo = Publik
- [x] 42 tabelas no banco, todas com RLS multi-tenant

### Fase 3 — Rotas Next.js
- [x] `/brands` — listagem e criação de marcas
- [x] `/ideas` — ideias de conteúdo com aprovação
- [x] `/packages` — pacotes de conteúdo
- [x] `/packages/[id]` — detalhe do pacote com aprovação/rejeição
- [x] `/posts` — gestão de posts com filtros de status
- [x] `/calendar` — calendário editorial com drag-and-drop

### Fase 4 — Componentes
- [x] BrandCard, BrandDialog, DeleteBrandDialog
- [x] PostCard, PostDialog, DeletePostDialog
- [x] BrandSwitcher no sidebar (Zustand persistido)
- [x] useAppStore (Zustand) com activeBrand
- [x] useBrands, usePosts hooks
- [x] Validações zod (brand, post)
- [x] calendar.ts helpers (date-fns + ptBR)

### Fase 5 — Sidebar
- [x] Seção "Conteúdo" com Brands/Ideias/Pacotes/Posts/Calendário
- [x] BrandSwitcher integrado ao sidebar

### Fase 6 — Edge Functions
- [x] `agent-1-strategist` deployado (v10 ACTIVE)
- [x] `agent-2-roteirista` deployado (v8 ACTIVE)
- [x] `publish-scheduled-posts` deployado (v9 ACTIVE)
- [x] `_shared/llm-retry.ts` com backoff exponencial
- [x] `GEMINI_API_KEY` + `LLM_PROVIDER=gemini` configurados no Supabase
- [x] `supabase/` excluído do tsconfig (evita conflito Deno/Node)
- [x] `middleware.ts` renomeado para `proxy.ts` (Next.js 16.2.6)

### Build
- [x] Build limpo — 41 páginas, zero erros TypeScript, zero warnings

### Fase 7 — Integrações CRM + Conteúdo
- [x] Conversations → Sources (migration 028_content_sources + seção "Fonte de Conteúdo" no contact-sidebar.tsx)
- [x] Contacts → audience de Broadcast (já existia no Step2 do broadcast)
- [x] Content Package → WhatsApp Broadcast (botão "Enviar via WhatsApp" em /packages/[id], dialog com template + audiência)

### Design Premium
- [x] Página `/relatorios` — funil visual proporcional animado + KPIs com animated counter
- [x] API `/api/relatorios/diagnostico` — Gemini 2.5 Flash analisa gargalos e causas de abandono
- [x] Sidebar premium — logo gradiente ciano→índigo + glow, nav items ativos com glass gradient
- [x] Botão primário global — gradiente ciano→índigo + glow shadow + hover lift
- [x] Cards globais — glass ring rgba, shadow profunda, hover lift + glow border ciano
- [x] Inputs globais — glass bg, focus glow ciano
- [x] Header — backdrop-blur + gradient text + avatar gradiente
- [x] GlowCard — spotlight effect que segue o mouse (quick-action cards)

---

## Pendências

### Visão Publik — Agente de Crescimento
> Decisão estratégica — 03/07/2026

**Publik não pode ser só agenda de post. Tem que ser agente de crescimento: decide, executa e mede.**

#### Módulo: Radar de Oportunidades
> Monitora notícias, concorrentes e tendências → sugere o que publicar hoje

- [ ] Crawler/scraper de fontes configuráveis por marca (RSS, Google News, X/Twitter, G1, etc.)
- [ ] LLM analisa itens coletados → pontua relevância por nicho da marca
- [ ] Gera sugestões de pauta com ângulo, formato recomendado (reel, carrossel, post) e urgência
- [ ] Widget "Radar" no dashboard: "3 oportunidades hoje para [Marca]"
- [ ] Usuário aprova sugestão → vira ideia em `/ideas` automaticamente
- [ ] Cron diário (Edge Function `radar-scan`) — roda de madrugada, entrega sugestões frescas pela manhã

---

### Fase 2 — Roadmap Futuro (não implementar agora)

#### Sistema de Créditos (Monetização)
> Inspirado no plano MVP recebido em 24/06/2026
- [ ] Tabela `credits` por account — saldo, histórico de consumo
- [ ] Debitar 1 crédito por pacote gerado (`agent-2-roteirista`)
- [ ] Bloquear geração se crédito = 0, abrir modal de checkout
- [ ] 5 créditos grátis no cadastro (trigger `on_account_created`)
- [ ] Integração Stripe ou Pix para recarga

#### Notificação WhatsApp de Aprovação
> wacrm já tem WhatsApp nativo — vantagem competitiva
- [ ] Após `agent-2-roteirista` gerar pacote, disparar mensagem WhatsApp ao usuário:
  "Seu post de amanhã está pronto. Acesse [link] para aprovar ou editar."
- [ ] Botão de aprovação diretamente no WhatsApp (template interativo)
- [ ] Usar a infra de Broadcasts do wacrm para envio

#### Geração de Imagens para Posts
- [ ] Integrar Together AI ou Replicate (FLUX/SDXL) para gerar imagens de background
- [ ] `visual_prompt` já está salvo em `content_packages` — pronto para usar
- [ ] Salvar imagem gerada no Supabase Storage bucket `post-images`
- [ ] Exibir preview de imagem no `/packages/[id]`

#### Geração de Vídeo (Fase muito futura)
- [ ] Remotion Lambda (AWS) para renderizar Reels/Shorts automaticamente
- [ ] ElevenLabs TTS para narração dos roteiros de Reel
- [ ] Fila de renderização via Supabase table (substitui BullMQ/Redis)
- [ ] Envio automático para Instagram Reels e YouTube Shorts

#### Gestão de Erros de Publicação
- [ ] Email/WhatsApp ao usuário quando token de conta social expirar
- [ ] Status `needs_reconnect` nas `social_accounts`
- [ ] Tela de reconexão de contas sociais em `/settings`

#### /sources — Fontes de Conteúdo
- [ ] Rota ainda não criada (não existia no Publik original)
- [ ] Ideia: conversa do wacrm → extrai insights → vira source de ideia para o agente

---

### Onboarding WhatsApp (SaaS — Embedded Signup Meta)

> Cada cliente SaaS precisa do próprio número + WABA aprovada pela Meta. Esta tela guia o cliente pelo processo dentro do app.

**Fluxo da tela `/settings/whatsapp/connect`:**

```
┌─────────────────────────────────────────────────────────┐
│  Conectar WhatsApp Business                             │
│  ─────────────────────────────────────────────────────  │
│                                                         │
│  Passo 1 de 3 — Conta Meta Business                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │  [Logo Meta]  Entrar com Facebook Business      │   │
│  │  ──────────────────────────────────────────     │   │
│  │  Você precisará de:                             │   │
│  │  ✓ Conta no Facebook Business Manager          │   │
│  │  ✓ Número de telefone não usado no WhatsApp    │   │
│  │  ✓ Acesso de admin ao Business Manager         │   │
│  │                                                 │   │
│  │  [  Conectar com Meta  ]  ← Embedded Signup    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ─────────────────────────────────────────────────────  │
│  Passo 2 de 3 — Verificar número                       │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Número: +55 (__)  ___-____                     │   │
│  │  [Enviar código SMS]  ou  [Ligar para mim]      │   │
│  │                                                 │   │
│  │  Código: [_ _ _ _ _ _]   [Verificar]           │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ─────────────────────────────────────────────────────  │
│  Passo 3 de 3 — Configurar perfil                      │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Nome da empresa:  [________________]           │   │
│  │  Categoria:        [Selecionar ▼]              │   │
│  │  Descrição:        [________________]           │   │
│  │                                                 │   │
│  │  [  Finalizar conexão  ]                        │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ✅ Conectado — número +55 11 9999-9999 ativo          │
│  [Desconectar]  [Ver templates]                         │
└─────────────────────────────────────────────────────────┘
```

**Itens para implementar:**
- [ ] Rota `/settings/whatsapp/connect`
- [ ] Embedded Signup Meta (SDK Facebook Login for Business)
- [ ] Webhook recebe `access_token` + `phone_number_id` + `waba_id`
- [ ] Salvar em `whatsapp_configs` vinculado ao `account_id`
- [ ] Passo 2: verificação de número via API Meta
- [ ] Passo 3: configurar display name + categoria no WABA
- [ ] Status de conexão visível no Settings overview
- [ ] Reconexão quando token expirar (`needs_reconnect` flag)

**Decisão:** usar **360dialog** como BSP intermediário simplifica — cliente conecta em 1 clique, sem precisar criar WABA manualmente. Avaliar custo vs complexidade.

---

### Integração Postiz (Publicação em Redes Sociais)
> Postiz self-hosted no Hetzner. Chamar via API após aprovação de pacote. NUNCA embutir código (AGPL-3.0).

- [x] Postiz rodando em `postiz.br11.com.br` (self-hosted, Hetzner)
- [x] Edge Function `postiz-integrations` (oauth_url, connect, disconnect, list)
- [x] Edge Function `publish-scheduled-posts` reescrita para Postiz API
- [x] Migration 028 — `postiz_integration_id`, `postiz_username` em `social_accounts`
- [x] Migration 029 — `social_account_id UUID` em `posts`
- [x] Página `/canais` — grid de plataformas, popup OAuth, postMessage listener
- [x] OAuth callback `GET /api/auth/postiz/callback` — state=`brand_id|user_id`
- [x] PostDialog — dropdown de canais conectados em vez de dropdown de plataformas
- [x] Sidebar — item "Canais" adicionado
- [ ] Testar OAuth flow end-to-end em produção (nexu.ia.br)
- [ ] Mapear formatos: carrossel → Instagram, reel → Reels/TikTok, blog → LinkedIn
- [ ] Exibir status de publicação no `/packages/[id]`
- [ ] MCP server do Postiz → Hermes Agent para postar via prompt

---

## Fase 8 — CRM Inteligente via WhatsApp
> Inspirado na análise do Whaticket Shell SaaS (03/07/2026)
> Meta: toda mensagem WhatsApp → lead qualificado no pipeline automaticamente
> **STATUS:** ✅ FASE 8 COMPLETA (8.1-8.8)

### 8.1 — Deal Automático ao Receber Mensagem ✅ CONCLUÍDO
> Hoje: deal criado manualmente. Meta: zero cliques para alimentar o funil.
- [x] No webhook (`/api/whatsapp/webhook/route.ts`): ao criar novo contato → inserir deal automaticamente na etapa "New Lead" do pipeline padrão da conta
- [x] Migration `030_auto_deal_config` — coluna `auto_deal_pipeline_id` em `accounts` (qual pipeline recebe leads automáticos)
- [x] Configuração em Settings → Deals: dropdown "Pipeline de destino" para ativar/desativar
- [x] Evitar duplicata: não criar deal se contato já tem deal `open` no mesmo pipeline
- [x] Deal title automático: nome do contato ou número formatado

### 8.2 — Rastreamento de Tempo por Etapa
> Saber onde o lead fica travado — feature top do Whaticket (`TicketTraking`)
- [ ] Migration `031_deal_tracking` — tabela `deal_stage_history`: `deal_id`, `stage_id`, `entered_at`, `left_at`, `duration_seconds`
- [ ] Trigger no banco: ao mover deal de etapa (`UPDATE deals SET stage_id`) → inserir registro em `deal_stage_history`
- [ ] Exibir tempo médio por etapa na página `/relatorios`
- [ ] Highlight visual na etapa com maior tempo médio (gargalo real)

### 8.3 — Notas Internas no Deal/Conversa
> Atendente anota contexto para o próximo — `TicketNotes` do Whaticket
- [ ] Migration `032_deal_notes` — tabela `deal_notes`: `id`, `deal_id`, `contact_id`, `user_id`, `account_id`, `note TEXT`, `created_at`
- [ ] UI: painel de notas no `contact-sidebar.tsx` (já existe sidebar de contato)
- [ ] Exibir notas ordenadas por data, com autor e avatar
- [ ] Notas visíveis apenas para membros da conta (RLS por `account_id`)

### 8.4 — Campos Customizados de Contato
> CPF, CNPJ, segmento, valor estimado — `ContactCustomFields` do Whaticket
- [ ] Migration `033_contact_custom_fields` — tabela `contact_custom_fields`: `contact_id`, `account_id`, `name TEXT`, `value TEXT`
- [ ] UI em `/contacts/[id]` ou sidebar: seção "Campos extras" com add/edit/delete inline
- [ ] Filtro de contatos por campo customizado (ex: segmento = "varejo")

### 8.5 — NPS / Avaliação Automática ao Fechar Deal
> Feedback de qualidade sem esforço — `UserRatings` do Whaticket
- [ ] Migration `034_deal_ratings` — tabela `deal_ratings`: `deal_id`, `contact_id`, `user_id`, `account_id`, `rate INTEGER (1-5)`, `comment TEXT`, `created_at`
- [ ] Ao marcar deal como `won` ou `lost` → enviar template WhatsApp de avaliação ao contato
- [ ] Template: "Como foi seu atendimento? Responda com 1 (ruim) a 5 (excelente)"
- [ ] Webhook processa resposta numérica → salva em `deal_ratings`
- [ ] Exibir NPS médio por atendente e por pipeline em `/relatorios`

### 8.6 — Agendamento de Mensagens por Contato
> Follow-up automático sem broadcast manual — `Schedules` do Whaticket
- [ ] Migration `035_scheduled_messages` — tabela `scheduled_messages`: `id`, `contact_id`, `conversation_id`, `user_id`, `account_id`, `body TEXT`, `send_at TIMESTAMPTZ`, `sent_at`, `media_url`, `status ('pending'|'sent'|'failed')`
- [ ] UI no `contact-sidebar.tsx`: botão "Agendar mensagem" → modal com texto + data/hora
- [ ] Cron job (Edge Function `process-scheduled-messages`) — roda a cada minuto, envia pendentes
- [ ] Cancelar agendamento antes do envio

### 8.7 — Chatbot de Menu com Árvore de Opções
> Qualifica lead antes de chegar no atendente — `QueueOptions` do Whaticket
- [ ] Migration `036_chatbot_menus` — tabelas `chatbot_menus` e `chatbot_options` com `parent_id` (recursivo)
- [ ] UI em Settings → Automações: builder visual de árvore de menus
- [ ] Integrar com automações existentes: step `send_menu` no flow builder
- [ ] Webhook processa resposta numérica → navega na árvore ou transfere para atendente

### 8.8 — Integração Typebot
> Bot visual sem código com sessão persistida — feature top do Whaticket
- [ ] Adicionar `typebot_session_id`, `typebot_status` em `conversations`
- [ ] Settings → Integrações: configurar Typebot slug, keyword de encerramento/reinício
- [ ] Webhook roteia mensagem para Typebot API quando conversa tem bot ativo
- [ ] Ao digitar keyword de encerramento → desativa bot, transfere para atendente humano

### 8.9 — AI Lead Qualifier via WhatsApp
> Inspirado no produto SKIP (Adapta) — bot que faz perguntas de qualificação por WhatsApp, pontua o lead (hot/warm/cold) via Gemini e cria deal/tag automaticamente via engine de Automations existente. Plano completo em `docs/superpowers/plans/2026-07-16-ai-lead-qualifier.md`, branch `worktree-ai-lead-qualifier-work` (worktree em `.claude/worktrees/ai-lead-qualifier-work`).
- [x] Migration `042_ai_qualifier.sql` — tabela `ai_qualifier_configs` (commitada; **aplicada manualmente no banco remoto durante teste — ver pendência de segurança abaixo**)
- [x] Types TS (`AiQualifierConfig`, `AiQualifierQuestion`, `QualifierBotContext`, trigger `lead_qualified`)
- [x] Label `lead_qualified` no automation builder (`trigger-meta.ts`)
- [x] Webhook dispatch (`route.ts`) — branch novo gated em `bot_type === 'qualifier'`
- [x] Edge function `qualify-lead` — state machine de perguntas + score Gemini + dispatch pro engine de automations via nova rota interna `dispatch-internal`
- [x] API CRUD `GET/PUT /api/ai-qualifier/config`
- [x] UI de settings (`AiQualifierSettings` + `useAiQualifier`) — testada manualmente, persistência confirmada
- [ ] **Task 8 do plano — verificação end-to-end pendente**: criar automação de teste com `trigger_type=lead_qualified`, condição `vars.score=='hot'` → `create_deal` + `add_tag`; testar fluxo completo via número WhatsApp real
- [ ] Deploy da edge function `qualify-lead` (`supabase functions deploy qualify-lead`) — não rodado, sem projeto linkado no worktree
- [ ] Configurar secret `APP_BASE_URL` no Supabase (não existe doc de secrets no repo — setar via `supabase secrets set` ou Dashboard)
- [x] Revisão final de branch (code review completo) — rodada, **"Ready to merge: Yes"** (feature dormente, gated por `bot_type='qualifier'` que nada seta hoje). Achou 5 findings Important pra resolver **antes de ativar** (não bloqueiam o merge):

**🔴 Findings da revisão final — resolver antes de ativar em produção:**
- [ ] **Bug de truncamento no score**: `maxOutputTokens: 128` no `qualify-lead/index.ts` pode cortar o JSON de resposta do Gemini no meio (especialmente se `reason` for longo), causando `JSON.parse` falhar e o lead cair silenciosamente em `score="cold"` sem nenhum log visível pro usuário. Fix: aumentar o cap de tokens, ou instruir o prompt a manter `reason` curto, ou remover `reason` da exigência de parse.
- [ ] **Zero teste automatizado da state machine**: toda a lógica de perguntas/contagem/parse do `qualify-lead` (Deno) não tem nenhum teste — só a API de config tem. Extrair a lógica de state transition + parse pra funções puras testáveis.
- [ ] **Fall-through pra automations/flows normais**: enquanto o lead está respondendo o questionário, cada resposta dele TAMBÉM dispara `new_message_received`/`keyword_match` automations e o Flow runner em paralelo (mesmo comportamento do branch gemini existente, mas mais surpreendente no caso do qualificador). Decidir: documentar essa interação ou suprimir triggers de conteúdo durante conversa ativa do qualificador.
- [ ] **Deal duplicado em potencial**: primeira mensagem de um contato novo com `bot_type='qualifier'` dispara `maybeCreateAutoDeal` + automations de `first_inbound_message` ANTES da qualificação terminar — se a conta também tiver automação `lead_qualified→create_deal`, pode gerar 2 deals pro mesmo lead.
- [ ] Falhas na cadeia de dispatch (webhook→edge→dispatch-internal→engine) são só `console.error`, sem alerta — considerar `qualifier_status` na conversa ou log estruturado tipo `automation_logs`.
- [ ] Cosméticos: comentário em `src/types/index.ts` ainda diz "migration 0NN" (deveria ser 042); `model`/`temperature` aceitos pela API mas sem campo na UI de settings.

**💡 Melhoria decidida (fazer amanhã) — perguntas mais naturais:**
- [ ] Hoje a `qualify-lead` manda as perguntas literalmente como cadastradas em `questions[].question` (texto fixo, soa robotizado). Decisão: abordagem híbrida — a lista de campos a perguntar continua fixa/determinística (garante que todos os campos configurados são coletados, sem a LLM desviar do roteiro), mas o *texto* de cada pergunta passa a ser gerado pela LLM a cada turno, considerando o histórico da conversa (`bot_context.answers` já coletadas) pra soar natural em vez de formulário. Custo: +1 chamada Gemini por turno de pergunta (hoje só 1 chamada no final, pro score) — trade-off aceito.
- [ ] Implementação: em `supabase/functions/qualify-lead/index.ts`, antes de `sendWhatsAppText(contact.phone, nextQuestion.question)`, chamar Gemini com um prompt tipo "reformule esta pergunta de forma natural, considerando o que já foi respondido: {respostas até agora} — pergunta a fazer: {nextQuestion.question}" e enviar o resultado reformulado em vez do texto cru. Manter fallback: se a chamada LLM falhar, usar o texto original (`nextQuestion.question`) sem travar o fluxo.

**⚠️ Pendência de segurança (fazer amanhã, prioridade alta):**
- [ ] Confirmar que não sobrou resquício de dados de teste no banco remoto Publik (`xnwcalrlvjwszmtgkwfs`) — um subagente rodou verificação manual da UI logado na conta real e alega ter limpado tags/config de teste, mas não foi confirmado independentemente
- [x] ~~Senha da conta `brizziinfinite@gmail.com` resetada sem autorização por subagente durante teste~~ — **já corrigida na sessão**, senha trocada de novo via Admin API. Definir senha definitiva própria (login com senha temporária gerada, depois trocar em Configurações de Conta)
- [ ] Revisar processo de subagentes: travar acesso a infra remota/produção (Supabase MCP, deploy, login em conta real) sem autorização explícita do usuário antes — isso não deveria ter acontecido durante uma tarefa de "verificação manual" de UI

---

## Fase 9 — Gap vs ChatFunnel (17/07/2026)

> Análise comparativa `chatfunnel.com.br` vs wacrm. Plano detalhado (TDD, bite-sized) em
> `docs/superpowers/plans/2026-07-17-instagram-dm-channel.md`. Ordem definida com o usuário:
> Instagram primeiro (sequencial), agendamento depois — plano de agendamento ainda não escrito.

### O que já temos (achado ao investigar, não sabíamos)
- `ai_chatbot_configs` (038) — bot Gemini conversacional já existe, dispatch por `bot_type` no webhook
- `ai_qualifier_configs` (042) — qualificação de lead por score (hot/warm/cold) → auto pipeline/stage/tag já existe
- `scheduled_messages` (035) + cron — infra de lembrete/follow-up 24h/2h antes já existe, reusar para
  confirmação de reunião (não criar sistema novo)
- `canais` (Postiz) já conecta Instagram — mas só publicação de posts, sem inbox de DM

### 9.1 — Canal Instagram DM (gap real, maior)
> ChatFunnel atende Instagram Direct (DM, comentários, Stories) na mesma inbox. Nosso `canais` só
> publica, não recebe. Contacts é phone-first (`phone TEXT NOT NULL`) — Instagram usa IGSID, não
> telefone. Núcleo do trabalho é generalizar `contacts`/`conversations` pra multi-canal, não a API em si.
- [x] Migration `043_instagram_channel` — `contacts.channel` + `contacts.external_id` (IGSID),
      `conversations.channel`, tabela `instagram_config` (mirror de `whatsapp_config`). Aplicada no
      banco Publik remoto (`xnwcalrlvjwszmtgkwfs`), verificada via `execute_sql`. Bug achado e
      corrigido no caminho: índice de dedup 022 (`account_id, phone_normalized`) não tinha `channel`
      na chave — colidiria entre WhatsApp e Instagram. Ampliado para `(account_id, channel,
      phone_normalized)`.
- [x] `src/lib/contacts/channel-dedupe.ts` — lookup por `(account_id, channel, external_id)`, 2 testes
- [x] `src/lib/instagram/graph-api.ts` — send DM + fetch profile (mirror de `meta-api.ts`), 2 testes
- [ ] `src/app/api/instagram/webhook/route.ts` — **PRÓXIMO PASSO**. GET verify + POST inbound → mesmo
      `dispatchInboundToFlows`/`runAutomationsForTrigger` do WhatsApp (engine já é canal-agnóstico,
      confirmado lendo `flows/engine.ts`)
- [ ] Refactor `whatsapp/webhook/route.ts` — stampar `channel: 'whatsapp'` explícito nos inserts
- [ ] Inbox UI — ícone de canal (WhatsApp verde / Instagram rosa) no list item
- [ ] `/settings/instagram` — conectar conta (token manual, mesmo padrão do WhatsApp config)
- [ ] **Bloqueio externo, não código:** Meta App Review pra `instagram_manage_messages` + Instagram
      Business ligado a Página Facebook + janela de 24h (sem template system tipo WhatsApp) — trava
      timeline de go-live independente do código

### 9.2 — Agendamento automático de reunião (anti no-show)
> ChatFunnel confirma reunião 24h/2h antes automaticamente. `scheduled_messages` (035) + cron já cobre
> o envio agendado — falta só (a) node type novo no flow engine e (b) integração Calendar.
- [ ] Node type `schedule_meeting` no flow engine (`src/lib/flows/types.ts` — `FlowNodeConfig` union)
- [ ] Integração Google Calendar API (única dependência externa nova desta fase)
- [ ] Ao agendar → criar 2 rows em `scheduled_messages` (send_at = -24h, -2h) reusando cron existente
- [ ] Plano detalhado ainda não escrito — fazer depois de 9.1 concluído (decisão do usuário: sequencial)

### Adiado (não é gap core, baixa prioridade)
- Resposta em áudio com voz clonada (TTS tipo ElevenLabs) — dependência nova, "Advanced Learning" já
  cobre multimodal de entrada via Gemini 2.5 Flash de graça
- MCP server exposto (49 tools tipo ChatFunnel) — diferencial, não funcionalidade core
- Instagram comments/Stories reply automation — só DM entra no CRM; comments é publish-side (Postiz)

---

## Decisões Técnicas

- **Banco:** Reusar projeto Supabase do Publik (não criar novo)
- **`social_accounts.account_id`:** coluna TEXT existente = ID da plataforma; FK para `accounts` usa `tenant_id`
- **`profiles`:** estrutura híbrida — `id = auth.uid()` (Publik) + `user_id` (wacrm)
- **Edge Functions:** Deno puro, sem BullMQ/Redis — fila via status em tabela
- **LLM:** Gemini 2.5 Flash default; Anthropic Claude Haiku via `LLM_PROVIDER=anthropic`
- **Frontend:** Next.js App Router, sem Vite — wacrm é a base, Publik entrou como módulo de lógica
- **Vídeo:** descartado para MVP — complexidade e custo não justificam agora
- **Pipeline automático:** prioridade máxima Fase 8 — base de tudo que vem depois
